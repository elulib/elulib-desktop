{
  "name": "elulib-desktop",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "tauri": "tauri",
    "sync-version": "node scripts/sync-version.js",
    "dev:win": "npm run sync-version && cargo tauri dev --target x86_64-pc-windows-msvc",
    "dev:mac": "npm run sync-version && cargo tauri dev --target aarch64-apple-darwin",
    "dev:mac-intel": "npm run sync-version && cargo tauri dev --target x86_64-apple-darwin",
    "build:win": "npm run sync-version && cargo tauri build --target x86_64-pc-windows-msvc",
    "build:mac": "npm run sync-version && cargo tauri build --target aarch64-apple-darwin",
    "build:mac-intel": "npm run sync-version && cargo tauri build --target x86_64-apple-darwin",
    "test": "npm run test:rust",
    "test:rust": "cd src-tauri && cargo test",
    "test:rust:unit": "cd src-tauri && cargo test --lib",
    "test:rust:integration": "cd src-tauri && cargo test --tests"
  },
  "author": "elulib",
  "license": "MIT",
  "description": "Application de bureau élulib",
  "devDependencies": {
    "@tauri-apps/api": "^2.9.0",
    "@tauri-apps/cli": "^2.9.3",
    "@tauri-apps/plugin-opener": "^2.5.2"
  }
}
